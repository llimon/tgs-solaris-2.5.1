diff -ruN a/src/Makefile b/src/Makefile
--- a/src/Makefile	2022-04-21 15:30:15.000000000 +0000
+++ b/src/Makefile	2025-09-19 18:48:01.683457409 +0000
@@ -3033,7 +3033,7 @@
 	    $(PERLLIB)/ExtUtils/typemap if_perl.xs >> $@
 
 auto/osdef.h: auto/config.h osdef.sh osdef1.h.in osdef2.h.in
-	CC="$(CC) $(OSDEF_CFLAGS)" srcdir=$(srcdir) sh $(srcdir)/osdef.sh
+	CC="$(CC) $(OSDEF_CFLAGS)" srcdir=$(srcdir) ksh $(srcdir)/osdef.sh
 
 auto/os_haiku.rdef: os_haiku.rdef.in
 	@echo creating $@
diff -ruN a/src/auto/configure b/src/auto/configure
--- a/src/auto/configure	2022-04-21 15:30:15.000000000 +0000
+++ b/src/auto/configure	2025-09-12 17:27:58.389967629 +0000
@@ -8156,10 +8156,10 @@
 
 { $as_echo "$as_me:${as_lineno-$LINENO}: result: $enable_multibyte" >&5
 $as_echo "$enable_multibyte" >&6; }
-if test "$enable_multibyte" != "yes"; then
-  as_fn_error $? "The multi-byte feature can no longer be disabled. If you have
-		a problem with this, discuss on the Vim mailing list." "$LINENO" 5
-fi
+#if test "$enable_multibyte" != "yes"; then
+#  as_fn_error $? "The multi-byte feature can no longer be disabled. If you have
+#		a problem with this, discuss on the Vim mailing list." "$LINENO" 5
+#fi
 
 { $as_echo "$as_me:${as_lineno-$LINENO}: checking --disable-rightleft argument" >&5
 $as_echo_n "checking --disable-rightleft argument... " >&6; }
diff -ruN a/src/float.c b/src/float.c
--- a/src/float.c	2022-04-21 15:30:15.000000000 +0000
+++ b/src/float.c	2025-09-18 20:15:37.180958718 +0000
@@ -12,6 +12,7 @@
  */
 #define USING_FLOAT_STUFF
 
+#include "my_isinf.h"
 #include "vim.h"
 
 #if (defined(FEAT_EVAL) && defined(FEAT_FLOAT)) || defined(PROTO)
@@ -341,6 +342,9 @@
 }
 
 # if defined(HAVE_MATH_H) || defined(PROTO)
+
+
+
 /*
  * "isinf()" function
  */
@@ -350,7 +354,8 @@
     if (in_vim9script() && check_for_float_or_nr_arg(argvars, 0) == FAIL)
 	return;
 
-    if (argvars[0].v_type == VAR_FLOAT && isinf(argvars[0].vval.v_float))
+//    if (argvars[0].v_type == VAR_FLOAT && isinf(argvars[0].vval.v_float))
+    if (argvars[0].v_type == VAR_FLOAT && isinf_float(argvars[0].vval.v_float))
 	rettv->vval.v_number = argvars[0].vval.v_float > 0.0 ? 1 : -1;
 }
 
Binary files a/src/gvim and b/src/gvim differ
diff -ruN a/src/json.c b/src/json.c
--- a/src/json.c	2022-04-21 15:30:15.000000000 +0000
+++ b/src/json.c	2025-09-18 20:20:09.437612157 +0000
@@ -13,7 +13,7 @@
  * Follows this standard: https://tools.ietf.org/html/rfc7159.html
  */
 #define USING_FLOAT_STUFF
-
+#include "my_isinf.h"
 #include "vim.h"
 
 #if defined(FEAT_EVAL) || defined(PROTO)
diff -ruN a/src/libvterm/Makefile b/src/libvterm/Makefile
--- a/src/libvterm/Makefile	2022-04-21 15:30:15.000000000 +0000
+++ b/src/libvterm/Makefile	2025-09-12 19:01:46.729272260 +0000
@@ -8,7 +8,7 @@
   LIBTOOL +=--quiet
 endif
 
-override CFLAGS +=-Wall -Iinclude -std=c99 -Wpedantic -DINLINE=""
+override CFLAGS +=-Wall -Iinclude -std=c99 -DINLINE=""
 
 ifeq ($(shell uname),SunOS)
   override CFLAGS +=-D__EXTENSIONS__ -D_XPG6 -D__XOPEN_OR_POSIX
diff -ruN a/src/libvterm/include/vterm.h b/src/libvterm/include/vterm.h
--- a/src/libvterm/include/vterm.h	2022-04-21 15:30:15.000000000 +0000
+++ b/src/libvterm/include/vterm.h	2025-09-07 20:45:36.287194926 +0000
@@ -16,9 +16,6 @@
 #define FALSE 0
 
 // from stdint.h
-typedef unsigned char		uint8_t;
-typedef unsigned short		uint16_t;
-typedef unsigned int		uint32_t;
 
 #define VTERM_VERSION_MAJOR 0
 #define VTERM_VERSION_MINOR 2
diff -ruN a/src/libvterm/src/encoding.c b/src/libvterm/src/encoding.c
--- a/src/libvterm/src/encoding.c	2022-04-21 15:30:15.000000000 +0000
+++ b/src/libvterm/src/encoding.c	2025-09-19 15:09:22.979313554 +0000
@@ -1,4 +1,5 @@
 #include "vterm_internal.h"
+#include "vterm.h"
 
 #define UNICODE_INVALID 0xFFFD
 
diff -ruN a/src/libvterm/src/vterm_internal.h b/src/libvterm/src/vterm_internal.h
--- a/src/libvterm/src/vterm_internal.h	2022-04-21 15:30:15.000000000 +0000
+++ b/src/libvterm/src/vterm_internal.h	2025-09-18 19:14:14.066006698 +0000
@@ -1,6 +1,7 @@
 #ifndef __VTERM_INTERNAL_H__
 #define __VTERM_INTERNAL_H__
 
+#include <stdint.h>
 #include "vterm.h"
 
 #include <stdarg.h>
diff -ruN a/src/my_isinf.h b/src/my_isinf.h
--- a/src/my_isinf.h	1969-12-31 17:00:00.000000000 +0000
+++ b/src/my_isinf.h	2025-09-18 20:12:38.753909023 +0000
@@ -0,0 +1,46 @@
+#ifndef MY_ISINF_H
+#define MY_ISINF_H
+
+#include <stdint.h>
+
+// Use a macro to provide a generic isinf that selects the right function.
+// The sizeof() operator is a compile-time constant.
+// The ?: operator is standard C, and the compiler will only evaluate the
+// branch that matches the type of `x`.
+#define isinf(x) \
+    (sizeof(x) == sizeof(float) ? isinf_float(x) : isinf_double(x))
+
+/*
+ * Custom isinf for single-precision float types.
+ */
+static inline int isinf_float(float x) {
+    union {
+        float f;
+        uint32_t i;
+    } u;
+    
+    u.f = x;
+    
+    // An infinite float has an exponent of all 1s (0xFF) and a mantissa of 0.
+    // The pattern for an all-ones exponent and all-zeros mantissa is 0x7F800000.
+    return (u.i & 0x7FFFFFFF) == 0x7F800000;
+}
+
+/*
+ * Custom isinf for double-precision float types.
+ */
+static inline int isinf_double(double x) {
+    union {
+        double d;
+        uint64_t i;
+    } u;
+
+    u.d = x;
+
+    // An infinite double has an exponent of all 1s (0x7FF) and a mantissa of 0.
+    // The pattern for an all-ones exponent and all-zeros mantissa is 0x7FF0000000000000.
+    return (u.i & 0x7FFFFFFFFFFFFFFF) == 0x7FF0000000000000;
+}
+
+#endif /* MY_ISINF_H */
+
diff -ruN a/src/strings.c b/src/strings.c
--- a/src/strings.c	2022-04-21 15:30:15.000000000 +0000
+++ b/src/strings.c	2025-09-18 20:20:04.257523619 +0000
@@ -12,6 +12,7 @@
  */
 
 #define USING_FLOAT_STUFF
+#include "my_isinf.h"
 #include "vim.h"
 
 /*
